# 基于 A2A 和 MCP 的个人智能健康助手 (Personal Health Assistant) - 项目进展报告

## 1. 项目概述

本项目旨在打造一个基于**多智能体架构 (Multi-Agent System)** 的私密、智能、贴心的个人健康管理助手。通过整合 OCR、LLM 和 RAG 技术，系统能够将用户分散的病历、检查报告、用药记录等信息转化为结构化的个人健康知识库。

在此基础上，我们构建了**微信小程序**与 **Web 端**双端应用，提供从日常健康咨询、用药提醒到辅助就医沟通的全方位智能服务。

**核心理念：** 本项目定位于**辅助性**健康管理工具，所有 AI 输出仅供参考，不替代专业医疗诊断。

---

## 2. 当前架构与技术栈

### 2.1 总体架构

系统采用 **Host-Agent 分布式架构**，由一个主控 Host Agent 负责任务分发，多个专用子 Agent 协同工作。

- **前端 (Frontend):**
  - **微信小程序:** 提供便捷的移动端入口，支持拍照 OCR、实时咨询、用药提醒。
  - **React Web:** 提供完整的大屏管理体验，用于复杂的病历整理和数据可视化。
- **后端 (Backend):**
  - **Host Agent:** 核心路由层，负责服务发现、鉴权与请求分发。
  - **Sub-Agents:** 独立部署的微服务智能体，包括：
    - `HealthRecordsManager` (健康档案管理员)
    - `HealthAdvisor` (健康顾问)
    - `MedicationReminder` (用药提醒助手)
    - `VisitSummaryGenerator` (就诊摘要生成器)
- **数据层 (Data Layer):**
  - **PostgreSQL:** 存储结构化健康数据、对话历史 (`chat_messages`)、用户信息。
  - **File System:** 存储 OCR 原始图片与生成的医疗文档。

### 2.2 关键技术

- **通信协议:**
  - **HTTP/REST:** 基础 API 交互。
  - **SSE (Server-Sent Events):** 实现低延迟的流式 AI 对话。
  - **JSON-RPC:** Agent 间的标准通信格式。
- **AI 与工具链:**
  - **MCP (Model Context Protocol):** 标准化的大模型工具调用协议。
  - **Direct Routing:** 混合路由机制（Host 代理 + 直连），优化实时对话性能。
  - **Thinking Process Visualization:** 可视化 AI 的思考与工具调用过程。

---

## 3. 已实现核心功能

### 3.1 多智能体协同系统

目前已成功部署并联调了四个核心智能体：

| 智能体名称         | 职责描述         | 核心能力 (MCP Tools)                                                          |
| :----------------- | :--------------- | :---------------------------------------------------------------------------- |
| **健康档案管理员** | 数据的录入与解析 | `OCRTool` (文档识别), `DataExtractionTool` (结构化提取), `StorageTool` (入库) |
| **健康顾问**       | 实时健康咨询     | `DiagnosisTool` (症状分析), `KnowledgeTool` (知识库问答)                      |
| **用药提醒助手**   | 用药计划管理     | `ReminderTool` (提醒设置), `NotificationTool` (消息推送)                      |
| **就诊摘要生成器** | 就诊前辅助       | `DocumentTool` (文档处理), `AIAnalysisTool` (摘要生成)                        |

### 3.2 微信小程序端

- **流式对话体验:** 实现了仿照 React 端的流式响应解析，支持显示 AI 的“思考过程”与“最终回复”。
- **核心流程已打通:**
  - **健康咨询:** 支持文本/语音输入，历史记录云端同步。
  - **用药提醒:** 可视化设置服药时间与服药状态管理；已接入订阅消息授权流程。
  - **档案管理:** 支持拍照上传病历，通过 OCR 自动识别并归档。
- **交互优化:** 解决了侧边栏冲突、输入框焦点丢失等原生交互问题。
- **就诊摘要:** 已支持就诊摘要上传识别与历史列表接口调用。

### 3.3 后端工程化优化

- **性能优化:** 实现了 Agent 直连路由，绕过 Host Agent 中转，显著降低了对话延迟。
- **稳定性增强:** 修复了 Agent 工具调用死循环问题（如诊断工具参数容错），增加了数据库连接池管理。
- **数据一致性:** 补全了 `chat_messages` 表结构，实现了多端对话记录的实时同步与持久化。

### 3.4 已知缺口（待实现/待对齐）

- **首页数据接入:** 首页“健康档案/用药/就诊摘要数量 + 最近活动”已通过 `/api/dashboard/stats` 接入真实数据；但“查看全部活动”仍为占位交互。
- **就诊摘要详情页:** 目前以列表页展开/预览为主，尚未提供独立详情页路由（单条摘要详情的产品形态待对齐）。
- **健康趋势页面:** 首页已支持“趋势指标 + 选中指标图表渲染”，但尚未提供独立 `health-trends` 页面。
- **异步趋势分析:** `HealthRecordsManager` 异步分析工具仍为 mock 实现，需替换为真实分析逻辑。
- **同步调用策略:** `A2AServer` 的 `agent.invoke()` 当前显式不支持同步调用，仅支持流式接口。

---

## 4. 近期更新与修复 (Latest Changelog)

- **[Feat] 知识库检索 Rerank:** 为 `HealthAdvisor` 的 `KnowledgeTool` 接入阿里云 DashScope TextReRank（二阶段重排），支持开关/候选集大小/超时配置与失败回退，提升 RAG 检索精度。
- **[Fix] 管理端刷新文档列表:** 修复 React 管理端刷新后“暂无文档”的问题，页面加载时自动触发检索并避免重复请求。
- **[Fix] 知识库文件上传 413:** 调整 Nginx `client_max_body_size` 与后端上传大小限制配置，支持较大 PDF/DOCX 等文件上传与入库流程。
- **[Fix] 诊断工具死循环:** 修复了 `HealthAdvisor` 调用 `diagnosis_tool` 时因缺少 `symptoms` 参数导致的无限重试问题，增加了参数容错机制。
- **[Feat] 小程序流式对话:** 重构了小程序端的 `agent_chat.js`，支持 SSE 流式解析，并实现了“思考中”状态的可视化。
- **[Feat] 历史记录同步:** 在后端补充了咨询历史的 CRUD 接口，确保小程序能正确加载和保存历史对话。
- **[Perf] 直连路由:** 优化了 `api.js` 中的服务发现逻辑，实现了对 Docker 内部服务名的自动解析与直连。
- **[Fix] 健康档案接口缩进错误:** 修复 `health_records_api.py` 中的缩进问题，避免后端启动/导入失败。
- **[Fix] 咨询历史过滤一致性:** 统一为 SQL 层按 tags 过滤 `summary/health_records` 类型历史，避免“先查再过滤”导致的分页偏差。

## 5. 下一步计划

1.  **端到端功能补齐:** 完成小程序“最近活动-查看全部”列表页、独立健康趋势页、就诊摘要独立详情页形态对齐。
2.  **异步分析与诊断接入:** 将趋势分析与诊断工具从 mock 迁移到真实模型/服务调用，并完善失败降级策略。
3.  **知识库增强 (RAG):** 基于“向量召回 + 关键词兜底 + Rerank”完善评测与调参（候选集、阈值、超时、回退策略），补齐检索日志与可观测性，持续提升准确性与稳定性。
4.  **多模态交互:** 增强对医疗影像（如 X 光片）的初步解读能力（依赖多模态大模型）。
5.  **安全审计:** 加强数据访问日志记录与敏感信息脱敏处理。
